RewriteEngine On

# Allow existing files/directories through untouched
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Route all other requests to the API front controller
RewriteRule ^ index.php [QSA,L]
